@using Resources
<div id="edit_query_access" class="well space-above">
	<div class="form-group row">
		<div class="col-lg-12">
			<label>Selecteer de query's waar gebruiker '{{account.FullName}}' toegang toe heeft.</label>
      <ul class="account-list list-unstyled">
        <li>
          <div class="row">
            <div class="col-lg-11">Query</div>
            <div class="col-lg-1">
              <div class="text-center">Bekijken</div>
            </div>
          </div>
        </li>
        <li data-ng-repeat="queryAccess in account.QueryAccess">
          <div class="row">
            <div class="col-lg-11">
              <span>{{queryAccess.Name}}</span>
            </div>
            <div class="col-lg-1">
              <div class="text-center">
                <input type="checkbox" value="{{queryAccess.Name}}" data-ng-model="queryAccess.CanReadSelected" data-ng-disabled="!user.isEditor" />
              </div>
            </div>
          </div>
        </li>
      </ul>
		</div>
	</div>
</div>